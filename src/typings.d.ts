export type DockerEnvType = {
  pgUser: string;
  pgHost: string;
  pgDatabase: string;
  pgPassword: string;
  pgPort: number;
  redisHost: string;
  redisPort: number;
  port: number;
  period: string;
};
